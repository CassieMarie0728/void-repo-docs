<!-- METADATA: {"source_path": "android/app/src/main/res/mipmap-anydpi-v26", "source_sha": "c0b566c8fceb8026d46b728898554481a344390f", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-08-11T22:49:27Z", "doc_type": "directory"} -->
[Documentation Home](../../../../../../README.md) > [android](../../../../../README.md) > [app](../../../../README.md) > [src](../../../README.md) > [main](../../README.md) > [res](../README.md) > [mipmap-anydpi-v26](./README.md) > **mipmap-anydpi-v26**


---


# mipmap-anydpi-v26

> **Directory:** `android/app/src/main/res/mipmap-anydpi-v26`

## Purpose

This directory contains adaptive launcher icon definitions targeted at Android API level 26 (and above). The XML resources here declare separate background and foreground layers so the system can mask and render the app icon in different shapes and contexts.

The files are minimal, declarative Android adaptive-icon resources that point to color and mipmap drawables used by the system when displaying the app's launcher icon (including a round variant).

## Files

| File | Description |
| --- | --- |
| `ic_launcher.xml` | An XML resource that defines an Android adaptive launcher icon for API level 26 and above, specifying a background layer referencing a color resource named ic_launcher_background and a foreground layer referencing a mipmap resource named ic_launcher_foreground. |
| `ic_launcher_round.xml` | An XML resource that defines an adaptive round launcher icon composed from a background drawable (referencing a color resource) and a foreground drawable (referencing a mipmap resource) for the app's round launcher icon variant. |

## Key Components

- **`Adaptive launcher icon (default)`** (in `ic_launcher.xml`) — This file provides the primary adaptive-icon definition the system uses for the app's standard launcher icon on API level 26+, and thus controls the composed background and foreground assets that determine the app's default appearance in launchers and the system UI.
- **`Adaptive round launcher icon`** (in `ic_launcher_round.xml`) — This file supplies the adaptive-icon definition specifically for round icon variants, allowing the system to render a round-shaped launcher icon using the declared background and foreground assets when a round mask is applied.

## Architecture Notes

Both files are standalone adaptive-icon XML resources placed in the same mipmap-anydpi-v26 resource directory. Each declares an adaptive-icon composed of a background and a foreground drawable (the summaries indicate these reference a color resource and a mipmap resource), but no within-directory imports or inheritance relationships were detected between the two files.

Because no internal links between files were reported, treat ic_launcher.xml and ic_launcher_round.xml as separate declarations serving different visual variants (default and round) rather than as interdependent resources; they both rely on external drawable/color assets referenced by name in their declarations.

## Extraction Quality Note

The contents for ic_launcher.xml and ic_launcher_round.xml were extracted as raw_source, so structural details are approximated from the raw resource summaries and claims about internal structure are hedged.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)



---

<sub>This README was generated by [DocBot](https://github.com/marketplace/docbot-by-woden) from the structural analysis of the files in this directory. AI-generated content can contain mistakes — verify against the source code before acting on architectural claims.</sub>
